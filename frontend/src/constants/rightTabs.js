/**
 * Ключи вкладок ПРАВОЙ панели, общие для всех разделов (в адресе — `?right=<key>`).
 *
 * Здесь живут только «сквозные» вкладки — те, что есть больше чем в одном
 * разделе. `INFO` есть везде (чат, база знаний, файлы) и всегда идёт первой:
 * это ответ на вопрос «что это за объект». Специфичные для базы знаний вкладки
 * (summary/contents) — в `docTabs.js`.
 *
 * Ключи общие намеренно: `?right=info` должен означать одно и то же в любом
 * разделе, чтобы состояние панели переживало переключение между ними.
 */
export const RIGHT_TAB = {
  INFO: 'info',
  ATTACHMENTS: 'attachments',
};
