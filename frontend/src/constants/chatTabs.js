/**
 * Вкладки ПРАВОЙ панели чата (в адресе — `?right=<key>`).
 *
 * Здесь только вкладки чата; сквозные — в `rightTabs.js`.
 */
import { RIGHT_TAB } from './rightTabs';

export const CHAT_TAB = {
  // Токены чата. Своей вкладкой, а не хвостом «Инфо»: набор читают как одно
  // целое и сверяют построчно со счётом провайдера, а среди названия и дат он
  // терялся — и рос, вытесняя из первого экрана то, ради чего «Инфо» открывают.
  USAGE: 'usage',
  // Сквозные ключи берём из RIGHT_TAB — те же вкладки есть в базе знаний и
  // файлах, и `?right=info` должен означать одно и то же во всех разделах.
  INFO: RIGHT_TAB.INFO,
  ATTACHMENTS: RIGHT_TAB.ATTACHMENTS,
  REPO: RIGHT_TAB.REPO,
};
