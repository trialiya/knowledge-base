/** Страница дерева KB (узлы в одном уровне). */
export const KB_PAGE_SIZE = 10;

/** «Все» дочерние — загрузка папки целиком для сортировки/экспорта. */
export const KB_FULL_PAGE = 1000;

/** Страница сообщений в чате (начальная загрузка и догрузка вверх). */
export const CHAT_PAGE_SIZE = 20;
