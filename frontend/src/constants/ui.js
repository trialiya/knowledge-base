// ─── Timings ─────────────────────────────────────────────────────────────────

/** Сколько мс показывать иконку «скопировано» перед возвратом к иконке копирования. */
export const COPY_DONE_MS = 1500;

// ─── Chat scroll ──────────────────────────────────────────────────────────────

/** Расстояние от нижней кромки списка (px), при котором считаем, что прилипли. */
export const SCROLL_STICK_THRESHOLD = 60;

/** Расстояние от верхней кромки (px), при котором начинаем догрузку старых сообщений. */
export const SCROLL_LOAD_THRESHOLD = 120;

// ─── Tooltip positioning (DocLinkTooltip) ────────────────────────────────────

/** Ширина тултипа предпросмотра документа (px). */
export const TOOLTIP_WIDTH = 300;

/** Зазор между триггером и тултипом (px). */
export const TOOLTIP_GAP = 8;

/** Оценочная высота тултипа до первого рендера (px), используется для выбора стороны. */
export const TOOLTIP_HEIGHT_ESTIMATE = 160;

// ─── Message display ──────────────────────────────────────────────────────────

/** Максимальная длина gist-превью аргументов тул-колла в плашке (символы). */
export const GIST_PREVIEW_LEN = 80;
