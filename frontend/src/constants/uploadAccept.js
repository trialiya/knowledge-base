// Фильтр диалога выбора файла для всех мест, где грузят вложение (панель
// вложений, скрепка в композере чата). Один список на всех: разъехавшиеся копии
// означают, что через одну точку входа файл загрузить можно, а через другую —
// нет, причём молча, ещё до всякого запроса.
//
// Ограничение чисто клиентское и подсказочное: диалог всё равно позволяет выбрать
// «все файлы», а что действительно принимается, решает бэкенд (AttachmentService).
export const UPLOAD_ACCEPT =
  'text/*,.md,.json,.yaml,.yml,.xml,.csv,.log,.sql,.gradle,.java,.js,.jsx,.ts,.tsx,.py,.go,.rs,.html,.css';
