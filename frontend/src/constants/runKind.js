// Чем занят чат, когда он занят. Значение приходит с бэка (GET /runs/active,
// ChatRunService.ActiveRun) и живёт в состоянии чата как runKind рядом с runId:
// занятость одна, но обращаться с ней вкладке нужно по-разному.
export const RUN_KIND = {
  // Ответ модели: идёт стрим, есть что останавливать, и сообщение пользователя
  // встаёт в очередь прогона вместо того, чтобы ждать его конца.
  GENERATION: 'GENERATION',
  // Всё остальное, что держит чат, — сжатие контекста (/compact), git-команда,
  // восстановление очереди на старте бэка. Останавливать нечего (своего прогона
  // у такой операции нет), очереди тоже нет — писать в чат можно только после.
  OPERATION: 'OPERATION',
};
