// Фикстура плашки «контекст сжат» (CompactNotice) — в трёх состояниях, которые отличаются
// ровно одним: что известно про экономию.
//
// Числа взяты с реального сжатия: 169k входа раунда против 12k, с которых начнётся следующий
// запрос. Отдельно — оценка (`~`, ответа после сжатия ещё не было) и плашка без замера вовсе,
// какой её видят чаты, сжатые версией без учёта токенов.

/** Экономия замерена с обеих сторон: раунд оплатил 169k, следующий запрос начнётся с 12k. */
export const measured = {
  conversationId: 'chat-1',
  messageId: 512,
  compact: { messages: 42, summaryChars: 3800 },
  savings: { before: 169_000, after: 12_000, estimated: false, percent: 93 },
  timestamp: '2026-08-25T12:00:00',
};

/** Ответа после сжатия ещё не было: «после» оценено по системной части и помечено «~». */
export const estimated = {
  ...measured,
  savings: { before: 169_000, after: 11_000, estimated: true, percent: 93 },
};

/** Раунд не измерен (эндпоинт без usage) — плашка остаётся прежней, без строки экономии. */
export const unmeasured = { ...measured, savings: null };
