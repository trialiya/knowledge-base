// Фикстура плашки «контекст сжат» (CompactNotice) — в трёх состояниях, которые отличаются
// ровно одним: что известно про экономию.
//
// Числа взяты с реального сжатия: раунд прочитал 169k, следующий запрос начнётся с 12k, а
// системная часть чата (промпт со схемами инструментов) — 9.8k. Плашка показывает только
// разговор, то есть 159.2k против 2.2k. Отдельно — оценка (`~`, ответа после сжатия ещё не
// было, справа стоит написанная сводка) и плашка без замера вовсе, какой её видят чаты,
// сжатые версией без учёта токенов.

/** Экономия замерена с обеих сторон: 159.2k разговора против 2.2k, оставшихся после сводки. */
export const measured = {
  conversationId: 'chat-1',
  messageId: 512,
  compact: { messages: 42, summaryChars: 3800 },
  savings: { before: 159_200, after: 2_200, estimated: false, percent: 99 },
  timestamp: '2026-08-25T12:00:00',
};

/** Ответа после сжатия ещё не было: справа написанная сводка (1.2k) и пометка «~». */
export const estimated = {
  ...measured,
  savings: { before: 159_200, after: 1_200, estimated: true, percent: 99 },
};

/** Раунд не измерен (эндпоинт без usage) — плашка остаётся прежней, без строки экономии. */
export const unmeasured = { ...measured, savings: null };
