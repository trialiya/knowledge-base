// Фикстура строки идущего прогона над полем ввода (RunStatus): таймер и прирост input.
//
// startedAt лежит в будущем нарочно: отрицательное «прошло» клампится в 0:00, и снимок не
// зависит от того, через сколько секунд после сборки фикстуры он снят. Живой отсчёт проверяет
// юнит-тест RunStatus.test.jsx с фейковыми часами — стенду хватает раскладки строки.
export const generatingRun = { startedAt: Date.now() + 3_600_000, inputGrowth: 6400 };
