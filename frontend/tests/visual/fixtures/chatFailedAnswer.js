/**
 * Фикстура упавшего ответа в ленте (chatPanel/messages/Message.jsx).
 *
 * Кнопка «повторить» показывается только там, где ответ помечен ошибкой и у
 * него есть режим повтора, — до этого кейса единственная кнопка ленты, которую
 * стенд не видел. Рядом стоит обычный ответ: разница между ними и есть то,
 * ради чего кейс нужен.
 */
export const failedAnswer = [
  {
    mid: 'm1',
    dbId: 401,
    sender: 'user',
    text: 'Собери отчёт по последним изменениям в indexer',
  },
  {
    mid: 'm2',
    dbId: 402,
    sender: 'ai',
    text: 'Не удалось получить ответ модели: превышено время ожидания (10 мин).',
    error: true,
    // RETRY_MODE.CONTINUE (constants/retryMode.js): вопрос сохранён, ответа нет
    // ни одного. Фикстуры сюда ничего из src не импортируют, поэтому значение
    // написано строкой — но взято оно оттуда, выдуманное кнопку бы не показало.
    retryMode: 'continue',
  },
  {
    mid: 'm3',
    dbId: 403,
    sender: 'ai',
    text: 'Готово — отчёт ниже. За неделю в `indexer` два изменения: размер пачки и повтор при сбое.',
  },
];
