/**
 * Фикстура перехода к найденному документу: что видно в центре базы знаний,
 * когда документ открыли из единого поиска по разделу
 * (components/knowledgeBasePanel/detail/DocumentDetail.jsx, общий
 * useAddressFind и FindBar, sectionAnchor.js).
 *
 * Совпадений намеренно несколько и в разных разделах, а адрес называет НЕ
 * первый из них: кейс про то, что бар встаёт на первое совпадение в названном
 * разделе, а не на первое в документе. Данные синтетические — документ
 * придуман, но по форме это ответ `GET /api/documents/{id}`.
 */

/** Запрос, с которым сюда пришли: он же стоит в адресе как `?find=`. */
export const query = 'grep';

/** Раздел из адреса (`?section=`), в форме бэкенда — путь заголовков через « > ». */
export const section = 'Инструменты > grepContent';

export const openedFromSearch = {
  node: {
    id: 41,
    type: 'document',
    title: 'Поиск по содержимому репозитория',
    description: [
      'Как grep ищет по репозиторию и где останавливается.',
      '',
      '# Инструменты',
      '',
      'Инструменты ходят в репозиторий через `GitService`.',
      '',
      '## grepContent',
      '',
      'Инструмент grep ходит в рабочее дерево, а с ревизией — в снимок коммита.',
      'Шаблон grep всегда регистронезависим.',
      '',
      '## listFiles',
      '',
      'Список файлов без содержимого.',
      '',
      '# Пределы',
      '',
      'Второй grep по неотслеживаемым идёт отдельным проходом.',
    ].join('\n'),
    createdAt: '2026-07-01T10:00:00',
    updatedAt: '2026-07-18T12:30:00',
  },
  path: [
    { id: 1, title: 'Проект', type: 'folder' },
    { id: 7, title: 'Анализ', type: 'folder' },
  ],
};
