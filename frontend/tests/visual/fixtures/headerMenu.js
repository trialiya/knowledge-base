/**
 * Фикстуры меню в правом верхнем углу шапки (common/layout/HeaderMenu.jsx).
 *
 * Данных у меню нет — пропсы это обработчики и два флага, — но стенду нужна
 * запись, чтобы снять кнопку-триггер и раскрытое меню. Флаг `showRefresh`
 * поднят: пункт «Обновить документ» есть только в базе знаний с открытым
 * документом, и без него меню в кадре короче настоящего.
 */

/** Меню базы знаний: с пунктом обновления, документ не обновляется. */
export const knowledgeBaseMenu = { showRefresh: true, refreshing: false };
