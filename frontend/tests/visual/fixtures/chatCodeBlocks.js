/**
 * Фикстура вариантов блока кода в ответе модели (chatPanel/messages/Message.jsx,
 * common/ui/CodeBlock.jsx). Собрана так, чтобы в одном пузыре встретились все
 * формы, которыми markdown задаёт код: заборчик с языком и без, однострочный
 * заборчик, блок отступом в четыре пробела и строчный код внутри абзаца.
 *
 * Смысл кейса — что шапку с языком и копированием получает КАЖДЫЙ блок: сама
 * разметка отличает блок от строчного кода только вложенностью в `pre`, и
 * однострочный заборчик без языка раньше оставался без неё.
 */
export const variants = [
  {
    mid: 'm1',
    dbId: 201,
    sender: 'user',
    text: 'Как запустить только фронтовые тесты?',
  },
  {
    mid: 'm2',
    dbId: 202,
    sender: 'ai',
    text: [
      'Ключ `kb.git.grep.timeout` задаётся в `application.yml`, а прогон — так:',
      '',
      '```bash',
      './run/test.sh front',
      './run/test.sh unit -- --tests "*ToolTranslationsTest"',
      '```',
      '',
      'Однострочный заборчик без языка:',
      '',
      '```',
      './gradlew :frontend:yarnServe',
      '```',
      '',
      'И блок отступом в четыре пробела:',
      '',
      '    KB_JAVA21=1 ./run/test.sh build',
    ].join('\n'),
  },
];
