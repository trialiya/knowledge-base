/**
 * Фикстуры общей строки-операции (components/common/OperationRow.jsx).
 *
 * Компонент принимает уже переведённые строки, поэтому фикстура — это его
 * пропсы в том виде, в каком их собирают группы «Администрирования»
 * (IndexOperations, BulkOperations). Иконка и onRun в фикстуру не входят:
 * первая — узел, второй — поведение.
 */

/** Переиндексация: primary-кнопка, без доп. контролов. */
export const reindexOperation = {
  title: 'Переиндексировать все документы',
  desc: 'Пересоздаёт эмбеддинги всех документов. Поиск может работать медленнее, пока индекс перестраивается.',
  labels: {
    run: 'Переиндексировать',
    running: 'Индексация…',
    doneBadge: 'готово',
    done: 'Переиндексация запущена.',
    errorBadge: 'ошибка',
    error: 'Не удалось запустить переиндексацию. Попробуйте позже.',
  },
  runVariant: 'primary',
};

/** Экспорт: ghost-кнопка и чекбокс между пояснением и результатом. */
export const exportOperation = {
  title: 'Экспорт всех документов',
  desc: 'Выгрузка дерева в серверную папку (DOCUMENTS_EXPORT_PATH): Markdown + метаданные, структура папок сохраняется',
  labels: {
    run: 'Выгрузить',
    running: 'Выгрузка…',
    doneBadge: 'готово',
    done: 'Документы выгружены в серверную папку экспорта.',
    errorBadge: 'ошибка',
    error: 'Не удалось выгрузить. Проверьте, что задан DOCUMENTS_EXPORT_PATH.',
  },
  runVariant: 'ghost',
};

/** Четыре состояния, которые должна показать одна и та же строка. */
export const operationStates = ['idle', 'running', 'done', 'error'];
