/**
 * Фикстура редактора документа
 * (components/knowledgeBasePanel/editor/MarkdownEditor.jsx).
 *
 * Снимок нужен ради панели инструментов: полтора десятка кнопок-значков в ряд,
 * с разделителями групп и правой группой (копирование, предпросмотр, история).
 * Текст в поле — повод показать, что панель стоит над ним, а не ради разметки.
 */

/** Короткий документ: заголовок, список, ссылка и блок кода. */
export const shortDocument = [
  '# Сборка фронтенда',
  '',
  'Проверки идут через `run/test.sh` — вручную gradle не собирают.',
  '',
  '- `front` — юнит-тесты',
  '- `harness` — снимки компонентов',
  '',
  '```bash',
  './run/test.sh front',
  '```',
].join('\n');
