/**
 * Фикстуры для списка чатов — содержимого левой панели раздела
 * (components/chatPanel/ChatList.jsx).
 *
 * Компонент читает у чата только id и title, но форма объекта та же, что в
 * fixtures/chatHeader.js (ответ GET /api/chats), — одна и та же фикстура должна
 * годиться и списку, и шапке. Id синтетические.
 */

/**
 * Несколько чатов: кнопка удаления в строке есть только на таком списке — на
 * единственном чате её нет, удалять последний нельзя (см. ChatList).
 */
export const severalChats = [
  { id: 'chat-1', title: 'История коммитов backend/build.gradle' },
  { id: 'chat-2', title: 'Как устроено хранение тул-коллов' },
  { id: 'chat-3', title: 'Новый чат' },
];

/** Единственный чат — строка без кнопок действий вовсе. */
export const singleChat = [severalChats[0]];
