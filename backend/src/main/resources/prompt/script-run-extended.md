### Before you write a script
You are not expected to write a non-trivial script from memory. Call `readSkill` with `"script-writing"` **first** — it returns when to prefer a script over single tools, how to structure one, worked examples to copy, and the pitfalls. Do it before the first `runScript` of a task, not after one came back wrong.
