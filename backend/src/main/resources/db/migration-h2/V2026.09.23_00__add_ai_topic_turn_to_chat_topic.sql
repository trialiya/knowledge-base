-- Номер хода (вопроса пользователя), на котором ИИ последний раз назвал чат
-- (AiTopicService). По нему пропущенная контрольная точка берётся на следующем
-- ответе, а не на следующей точке. NULL у чатов, названных до этой колонки, —
-- они будут названы заново на ближайшем ответе.
ALTER TABLE chat_topic ADD COLUMN ai_topic_turn INTEGER;
