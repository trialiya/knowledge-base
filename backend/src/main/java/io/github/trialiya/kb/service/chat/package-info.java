/**
 * Разговор целиком: {@code run} — рантайм прогона, {@code memory} — история и вызовы инструментов,
 * {@code event} — транспорт событий на вкладки, {@code script} — песочница инструмента {@code
 * runScript}, {@code prompt} — тексты, из которых собирается промпт, {@code context} — вложения и
 * опись приложенного, {@code topic} — сам чат и поиск по чатам, {@code git} — след git-команд
 * пользователя в истории.
 *
 * <p>Зависимости между ними идут в одну сторону: {@code event} ← {@code memory} ← {@code run}.
 * Транспорт событий поэтому и живёт отдельно — публикуют в него все, а знать про рантайм прогона
 * ради одного {@code publish} не должен никто.
 *
 * <p>Здесь остаётся только то, что не принадлежит ни одной из этих частей: {@code
 * ToolCatalogService} описывает не разговор, а набор инструментов, который панели настроек нужно
 * показать ровно таким, каким его видит модель.
 */
@NullMarked
package io.github.trialiya.kb.service.chat;

import org.jspecify.annotations.NullMarked;
