/**
 * Счёт токенов за весь чат: то, чего не собрать по загруженной странице ленты.
 *
 * <p>Отдельно от {@code memory} намеренно. Тот пакет отвечает на вопрос «что поедет модели», и
 * читает историю ради окна; здесь история читается ради денег — по всем рядам сразу, включая те,
 * что модели давно не едут. Общее у них только хранилище.
 *
 * <p>Направление зависимостей то же, что у соседей ({@code event} ← {@code runtime} ← {@code
 * memory} ← {@code run}): пакет не знает ни об одном из них и живёт на репозитории.
 */
@NullMarked
package io.github.trialiya.kb.service.chat.usage;

import org.jspecify.annotations.NullMarked;
