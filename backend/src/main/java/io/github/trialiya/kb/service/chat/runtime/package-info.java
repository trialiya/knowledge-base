/**
 * Живое состояние чата: то, что существует ровно столько, сколько идёт операция над ним, и исчезает
 * вместе с ней. {@link ConversationSlots} — заявка на чат (кто его занял), {@link RunRegistry} —
 * всё, что знает о себе идущий прогон.
 *
 * <p>Отдельно от {@code run} потому, что владелец этого состояния один, а пользователей много:
 * заявку берут и сжатие контекста, и git-команда, и восстановление очереди; счёт токенов ведёт
 * advisor изнутри tool-цикла, нумерацию вызовов — запись истории. Ради двух строк каждому из них
 * пришлось бы завязаться на рантайм прогона целиком.
 */
@NullMarked
package io.github.trialiya.kb.service.chat.runtime;

import org.jspecify.annotations.NullMarked;
