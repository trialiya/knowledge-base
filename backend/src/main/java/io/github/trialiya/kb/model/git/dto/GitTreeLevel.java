package io.github.trialiya.kb.model.git.dto;

import java.util.List;

/**
 * Листинг одного каталога — уровень дерева файлового браузера.
 *
 * <p>Нужен только для пакетной выдачи ({@link GitPathView#tree()}): чтобы раскрыть дерево до
 * глубоко вложенного файла, клиенту нужны листинги всех каталогов-предков, и одиночный {@code GET
 * /api/git/tree} на каждый уровень превращался в цепочку последовательных запросов.
 *
 * @param path путь каталога относительно корня репозитория ("" — корень)
 * @param nodes прямые потомки каталога (каталоги перед файлами, затем по алфавиту)
 */
public record GitTreeLevel(String path, List<GitFileNode> nodes) {}
