/**
 * Занятость чата: одна заявка на все длительные операции с его историей. Отдельно от {@code run}
 * потому, что заявку берёт не только генерация — сжатие контекста, git-команда пользователя и
 * восстановление очереди после перезапуска держат ту же самую; ради неё им пришлось бы завязаться
 * на рантайм прогона целиком.
 */
@NullMarked
package io.github.trialiya.kb.service.chat.slot;

import org.jspecify.annotations.NullMarked;
