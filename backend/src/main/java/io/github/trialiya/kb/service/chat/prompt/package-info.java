/**
 * Тексты, из которых собирается промпт: расширенная часть системного промпта ({@code
 * SystemPromptService}), описание проекта прогона ({@code ProjectPromptService}), инструкции режима
 * ({@code ChatModeService}) — и библиотека фраз, то есть заготовки для сообщения пользователя.
 */
@NullMarked
package io.github.trialiya.kb.service.chat.prompt;

import org.jspecify.annotations.NullMarked;
