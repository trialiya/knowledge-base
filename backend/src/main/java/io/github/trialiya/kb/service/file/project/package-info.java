/**
 * Какие проекты вообще есть и какой из них имеется в виду: {@code ProjectCatalog} (конфигурация,
 * разобранная один раз при старте, и единственный ответ на «проект не назван») и разовый бэкафилл,
 * вписывающий проект в сохранённые ссылки на файлы.
 */
@NullMarked
package io.github.trialiya.kb.service.file.project;

import org.jspecify.annotations.NullMarked;
