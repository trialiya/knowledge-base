## Сущности

### DocumentEntity
Таблица: `documents`. Основная сущность — документ или папка.

| Поле | Тип | Описание |
|---|---|---|
| `id` | Long | Первичный ключ |
| `title` | String | Название |
| `type` | String | `"document"` или `"folder"` |
| `parentId` | Long | FK → documents.id (null для корня) |
| `description` | String | Markdown-содержимое (null для папок) |
| `updatedAt` | LocalDateTime | Время последнего изменения |
| `position` | int | Порядок среди siblings (0-based) |
| `isSystem` | boolean | Системные узлы нельзя удалить/переименовать |
| `version` | int | `@Version` — optimistic locking. Инкрементится при любом save() |
| `summary` | String | AI-саммаризация description (null пока не сгенерирована) |
| `summarySourceVersion` | Integer | Значение `descriptionVersion` на момент генерации summary |
| `descriptionVersion` | int | Инкрементится **только** при изменении description. Стартует с 1 |

**Stale-механика:** `isSummaryStale()` → `summarySourceVersion < descriptionVersion`. Rename/move/reorder не делают summary устаревшим.

### Индексы

| Индекс | Таблица | Колонка | Примечание |
|---|---|---|---|
| `documents_parent_id_index` | `documents` | `parent_id` | Ускоряет поиск детей папки |
| `documents_description__index` (GIN trigram) | `documents` | `description` | **Дропнут** в `V2026.05.30` — мешал HOT-обновлениям, вызывал раздувание TOAST |

**Autovacuum-тюнинг** (`V2026.05.30`): на `documents` и TOAST — `scale_factor=0.0`, `threshold=20`, `cost_delay=0` (агрессивная очистка dead tuples).

### DocumentHistoryEntity
Таблица: `document_history`. Иммутабельный снепшот документа **перед** каждым обновлением.

| Поле | Тип | Описание |
|---|---|---|
| `id` | Long | PK |
| `documentId` | Long | FK → documents.id |
| `version` | int | Версия документа на момент снепшота |
| `title` | String | Название на момент снепшота |
| `type` | String | Тип на момент снепшота |
| `description` | String | Содержимое на момент снепшота |
| `updatedAt` | LocalDateTime | Временная метка |
| `summary` | String | AI-summary на момент снепшота |
| `summarySourceVersion` | Integer | |
| `descriptionVersion` | int | |

Строки никогда не мутируются после вставки — нет `@Version`.

### DocumentEmbeddingEntity
Таблица: `document_embeddings`. Один embedding на документ (UNIQUE по documentId).

| Поле | Тип | Описание |
|---|---|---|
| `id` | Long | PK |
| `documentId` | Long | FK → documents.id (UNIQUE) |
| `embedding` | float[] | Вектор, маппится в pgvector через `FloatArrayToVectorConverter` |
| `model` | String | Модель эмбеддинга, например `"text-embedding-3-small"` |
| `updatedAt` | OffsetDateTime | Время последнего обновления |

---

## DTO

### Document
Полный DTO для ответов API (GET /api/documents/{id}). Содержит `description`, `children`, `summary`.

| Поле | Тип |
|---|---|
| `id` | long |
| `title` | String |
| `type` | String |
| `parentId` | Long |
| `version` | int |
| `descriptionVersion` | int |
| `description` | String |
| `updatedAt` | LocalDateTime |
| `children` | List\<Document\> |
| `summary` | String |
| `summaryStale` | boolean |
| `summarySourceVersion` | Integer |

Метод `toDocumentShort()` → `DocumentShort`.

### DocumentShort
Лёгкий DTO для ответов create/update/move. **Не содержит `description`** — только метаданные. Реализует `ToolCallResponseItem` и `ToolCallResultMetaProvider`.

| Поле | Тип |
|---|---|
| `id` | long |
| `title` | String |
| `type` | String |
| `parentId` | Long |
| `version` | int |
| `descriptionVersion` | int |
| `updatedAt` | LocalDateTime |
| `summaryStale` | boolean |
| `summarySourceVersion` | Integer |

`getResultMeta()` возвращает: `id`, `type`, `title`, `parent`, `version`, `descriptionVersion`, `updated`.

### DocumentNode
Полный DTO для `GET /api/documents/{id}` и tree/children. Включает `description`, `children`, `hasChildren`, `system`. Реализует `ToolCallResponseItem` и `ToolCallResultMetaProvider`.

| Поле | Тип |
|---|---|
| `id` | long |
| `title` | String |
| `type` | String |
| `parentId` | Long |
| `version` | int |
| `description` | String |
| `descriptionVersion` | int |
| `updatedAt` | LocalDateTime |
| `children` | List\<DocumentNode\> |
| `hasChildren` | boolean |
| `system` | boolean |
| `summary` | String |
| `summaryStale` | boolean |
| `summarySourceVersion` | Integer |

`getResultMeta()` возвращает: `id`, `type`, `title`, `parent`, `version`, `descriptionVersion`, `updated`.

### DocumentHistoryShort
Лёгкий DTO для списка версий в `GET /api/documents/{id}/history`. **Не содержит `description`** — только метаданные. Полный текст версии загружается отдельно через `GET /api/documents/{docId}/history/{version}`.

| Поле | Тип |
|---|---|
| `documentId` | long |
| `version` | int |
| `descriptionVersion` | int |
| `title` | String |
| `type` | String |
| `updatedAt` | LocalDateTime |

### DocumentHistory
Полный DTO для конкретной версии истории. Возвращается эндпоинтом `GET /api/documents/{docId}/history/{version}`. Содержит `description` и все метаданные.

| Поле | Тип |
|---|---|
| `documentId` | long |
| `version` | int |
| `title` | String |
| `type` | String |
| `description` | String |
| `updatedAt` | LocalDateTime |
| `summary` | String |
| `descriptionVersion` | int |

### SearchResult
Результат поиска. Record с хлебными крошками. Реализует `ToolCallResponseItem` и `ToolCallResultMetaProvider`.

| Поле | Тип |
|---|---|
| `id` | Long |
| `title` | String |
| `snippet` | String |
| `updatedAt` | LocalDateTime |
| `summary` | String |
| `parentList` | List\<Parent\> |

**Parent** — вложенный record: `id` (long), `title` (String). Содержит предков от корня до родителя результата (не включая сам результат). Заполняется одним батчевым CTE-запросом `findAncestorsByIds` в `DocumentRepositoryCustomImpl`.

`getResultMeta()` возвращает: `id`, `title`.

### CreateDocumentRequest
| Поле | Тип | По умолчанию |
|---|---|---|
| `title` | String | — |
| `type` | String | `"document"` |
| `parentId` | Long | null |
| `description` | String | null |

### UpdateDocumentRequest
| Поле | Тип |
|---|---|
| `title` | String (опционально) |
| `description` | String (опционально) |

### MoveRequest
Record для `PATCH /api/documents/{id}/move`. Заменяет удалённые `MoveToParentRequest` и `ReorderRequest` — клиент указывает одного соседа вместо всего списка siblings.

| Поле | Тип |
|---|---|
| `parentId` | Long (null → корень) |
| `afterId` | Long (null → первым на уровне) |

### PagedChildren
Record для пагинированных children.

| Поле | Тип |
|---|---|
| `items` | List\<DocumentNode\> |
| `totalElements` | long |
| `totalPages` | int |
| `page` | int |
| `size` | int |
| `hasNext` | boolean |

---

## Репозиторий

### DocumentRepository
Основной репозиторий документов. Ключевые методы:

| Метод | Описание |
|---|---|
| `findByParentId(Long)` | Дети папки, сортировка по `position` |
| `findRoots()` | Корневые узлы |
| `findDescendantIds(Long)` | Рекурсивный CTE — все потомки |
| `findAncestorIds(Long)` | Рекурсивный CTE — предки от корня до родителя |
| `findSystemIdsByIdIn(List<Long>)` | Фильтр системных узлов |
| `shiftPositionsFrom(Long parentId, int fromPosition, long movedId)` | Сдвиг позиций ≥ fromPosition на +1 (открытие слота при кросс-уровневом перемещении) |
| `findMinPosition(Long parentId)` | Минимальная позиция на уровне (0 если пусто) |
| `shiftWindowUp(Long parentId, int newPos, int oldPos)` | Оконный сдвиг [newPos, oldPos) на +1 (перемещение вверх внутри уровня) |
| `shiftWindowDown(Long parentId, int oldPos, int anchorPos)` | Оконный сдвиг (oldPos, anchorPos] на -1 (перемещение вниз внутри уровня) |

**Удалён** `batchUpdatePositions(Map<Long, Integer>)` — заменён оконными сдвигами.

### DocumentRepositoryCustom / DocumentRepositoryCustomImpl
Содержит только `findAncestorsByIds(Collection<Long>)` — батчевое разрешение хлебных крошек для поисковых результатов. `batchUpdatePositions` удалён.

---

## DocumentService.move()

Унифицированный метод `move(long id, Long targetParentId, Long afterId)` заменяет пару `moveToParent` + `reorder`. Клиент передаёт одного соседа (`afterId`) вместо полного списка siblings — сервер вычисляет точный слот из актуального состояния БД.

**Стратегия позиционирования (допускаются гэпы):**

- **Перемещение внутри уровня вверх** — оконный сдвиг `[newPos, oldPos)` на +1, узел занимает `anchorPos + 1`
- **Перемещение внутри уровня вниз** — оконный сдвиг `(oldPos, anchorPos]` на -1, узел занимает `anchorPos`
- **Перемещение на другой уровень** — открытие слота через `shiftPositionsFrom`, дыра в исходном уровне остаётся

**Валидация:** 400 (цикл или `afterId == id`), 403 (системный узел), 404 (узел/родитель/afterId не найден), 409 (конкурентная модификация), 422 (цель не папка или afterId с другого уровня).

---

## Связи

- `documents.parentId` → `documents.id` (само-ссылка, дерево)
- `document_history.documentId` → `documents.id`
- `document_embeddings.documentId` → `documents.id` (UNIQUE)
- `attachments.documentId` → `documents.id` (когда ownerType = 'document')
