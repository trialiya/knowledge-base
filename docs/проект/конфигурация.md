# Конфигурация

## Основные параметры (`application.yaml`)

| Параметр | Значение по умолчанию | Описание |
|---|---|---|
| `server.port` | `8080` | Порт backend-сервера |
| `spring.datasource.url` | `jdbc:postgresql://localhost:5432/knowledgebase` | URL подключения к PostgreSQL |
| `spring.datasource.username` | `knowledgebase` | Имя пользователя БД |
| `spring.datasource.password` | `knowledgebase` | Пароль БД |
| `spring.servlet.multipart.max-file-size` | `1MB` | Максимальный размер загружаемого файла |
| `spring.servlet.multipart.max-request-size` | `2MB` | Максимальный размер запроса |
| `spring.mvc.async.request-timeout` | `30m` | Таймаут асинхронных запросов (AI-саммаризация, SSE) |
| `spring.flyway.enabled` | `true` | Включение миграций Flyway |
| `spring.flyway.validate-on-migrate` | `false` | Отключение валидации при миграции |
| `spring.threads.virtual.enabled` | `true` | Виртуальные потоки (Project Loom) |
| `spring.ai.retry.max-attempts` | `2` | Максимальное число попыток при сбоях AI |
| `kb.documents.export-path` | `${DOCUMENTS_EXPORT_PATH}` | Путь для экспорта документов |
| `kb.documents.replace` | `true` | Замена существующих файлов при экспорте |
| `kb.system-prompt.prompt` | `classpath:prompt/sys.md` | Системный промпт (справочник) для всех моделей |
| `kb.system-prompt.extended-prompt` | `classpath:prompt/sys-extended.md` | Расширенный системный промпт (обучение) для слабых моделей |

### Безопасность (`kb.security.*`)

| Параметр | Значение по умолчанию | Описание |
|---|---|---|
| `kb.security.username` | `${KB_SECURITY_USERNAME:admin}` | Логин единственного пользователя (HTTP Basic) |
| `kb.security.password` | `${KB_SECURITY_PASSWORD:admin}` | Пароль (BCrypt-хэшируется) |

### HikariCP (пул соединений)

| Параметр | Значение | Описание |
|---|---|---|
| `spring.datasource.hikari.minimum-idle` | `1` | Минимальное число простаивающих соединений |
| `spring.datasource.hikari.maximum-pool-size` | `10` | Максимальный размер пула |
| `spring.datasource.hikari.idle-timeout` | `300000` | Таймаут простоя (5 минут) |

### Поиск (`kb.search.*`)

| Параметр | Значение | Описание |
|---|---|---|
| `kb.search.keyword.limit` | `20` | Лимит результатов keyword-поиска |
| `kb.search.semantic.enabled` | `true` | Включает/отключает семантический поиск |
| `kb.search.semantic.threshold` | `0.20` | Порог семантической схожести |
| `kb.search.semantic.limit` | `20` | Лимит результатов семантического поиска |
| `kb.search.hybrid.keyword-weight` | `0.4` | Вес keyword-результатов (0..1) |
| `kb.search.hybrid.semantic-weight` | `0.6` | Вес semantic-результатов (0..1) |
| `kb.search.hybrid.threshold` | `0.20` | Минимальная семантическая схожесть |
| `kb.search.hybrid.limit` | `20` | Лимит результатов гибридного поиска |

### Sub-agent поиска (`kb.search.subagent.*`)

| Параметр | Значение | Описание |
|---|---|---|
| `kb.search.subagent.enabled` | `${KB_SEARCH_SUBAGENT_ENABLED:true}` | Включить/отключить searchCodebase |
| `kb.search.subagent.model-id` | `${KB_SEARCH_SUBAGENT_MODEL:${AI_MODEL}}` | Модель (может отличаться от основной) |
| `kb.search.subagent.max-tokens` | `${KB_SEARCH_SUBAGENT_MAX_TOKENS:12000}` | Макс. токенов на вызов |
| `kb.search.subagent.max-iterations` | `${KB_SEARCH_SUBAGENT_MAX_ITERATIONS:30}` | Жёсткий лимит раундов |
| `kb.search.subagent.allowed-tools` | `[grepContent, searchFiles, getFileTree, getFileOutline, getFileContent, searchDocuments, findDocumentsByName, getDocument, getTreeSkeleton]` | Read-only инструменты (без `searchCodebase`) |

### Проекты (`kb.projects`)

Репозитории, с которыми работает ассистент. Включённых записей может быть несколько: каждая — свой
репозиторий со своим монтированием. Вызов, который проект не назвал (сводка, фоновая задача, чат,
который проект не выбирал), получает **первый** проект списка.

Проект адресуется по `id` на всём пути: чат хранит выбранный (`chat_topic.project` — записывается
только вместе с отправленным сообщением и означает «на каком проекте шла история», поэтому
сохранённый, но выбывший из конфигурации id заменяется дефолтным на первом же сообщении), прогон
везёт его в `ToolContext`
(`projectId`), инструменты и `/api/git/**` резолвят репозиторий через `GitRegistry`, ссылки на файлы
и чипы композера несут проект в адресе. Смена проекта в чате помечается маркером в истории — см.
[API Reference](api-reference.md).

Записи с `enabled: false` не считаются и не проверяются — так проект и готовят заранее: блок лежит
в конфигурации (путь может быть ещё не смонтирован), пока флаг не переключат. Для приложения
выключенного проекта нет: репозиторий не открывается, в `GET /api/chats/projects` его нет, вызов с
его id отклоняется. Чат, который его выбрал, сохраняет id и едет на дефолтном проекте — по этому
расхождению фронт и понимает, что нужно сказать «проект больше недоступен».

**Каждому включённому проекту нужен свой mount и своё исключение `git safe.directory`** (глобов git
не понимает): рабочее дерево принадлежит хостовому пользователю, а контейнер работает из-под `kb`, и
`git grep` — subprocess. Исключения добавляет `docker/start.sh` при старте, читая те же переменные,
что и бэкенд (`PROJECT_PATH`, `PROJECT2_PATH`, `KB_PROJECTS_<i>_PATH`), — путь, положенный мимо
рекомендуемой схемы `/projects/<id>`, тоже получит своё. Не
доехавший mount стоит своего проекта, а не сервера: репозиторий не открывается, в логе — `ERROR`,
вызовы к этому проекту отклоняются по имени, остальные работают. Исключение — **первый** проект:
без него не работает ничего, и старт честно падает.

```yaml
kb:
  projects:
    - id: default                          # [a-z0-9._-], попадёт в URL как есть
      label: ${PROJECT_LABEL:Project}      # опционально; по умолчанию = id
      path: ${PROJECT_PATH:${kb.git.project-path}}
      edit-enabled: ${KB_GIT_EDIT_ENABLED:false}   # правка задаётся для каждого проекта отдельно
      untracked-edit-enabled: ${KB_GIT_UNTRACKED_EDIT_ENABLED:false}  # правка untracked-файлов
      allow-globs: []                      # untracked-файлы, видимые ассистенту (Ant-глобы)
      git-commands:                        # git-команды, доступные ПОЛЬЗОВАТЕЛЮ в интерфейсе
        enabled: ${KB_GIT_COMMANDS_ENABLED:false}
        push-enabled: ${KB_GIT_PUSH_ENABLED:false}
    - id: ${PROJECT2_ID:billing}           # слот второго проекта, по умолчанию выключен
      label: ${PROJECT2_LABEL:}
      path: ${PROJECT2_PATH:/projects/billing}
      edit-enabled: ${PROJECT2_EDIT_ENABLED:false}
      untracked-edit-enabled: ${PROJECT2_UNTRACKED_EDIT_ENABLED:false}
      allow-globs: []
      git-commands:
        enabled: ${PROJECT2_GIT_COMMANDS_ENABLED:false}
        push-enabled: ${PROJECT2_GIT_PUSH_ENABLED:false}
      enabled: ${PROJECT2_ENABLED:false}
```

**Как включить второй проект** (слот для него уже есть в поставляемом `application.yaml`):

1. `PROJECT2_ENABLED=true` и `PROJECT2_PATH_MOUNT=/абсолютный/путь/на/хосте` в `.env`;
2. раскомментировать mount `${PROJECT2_PATH_MOUNT}:/projects/billing:ro` в `docker-compose*.yaml`;
3. другой `id` — задать `PROJECT2_ID` и привести к нему путь монтирования и `PROJECT2_PATH`
   (`/projects/<id>`).

**Третий и следующие** — дописать запись в `kb.projects` (и mount) либо задать **весь** список
переменными `KB_PROJECTS_<i>_*`: Spring не сливает элементы списка из разных источников, и половина
списка из переменных окружения не дополняет yaml, а роняет старт на биндинге (проверено).

Недоступный проект (репозиторий не открылся) остаётся в `GET /api/chats/projects` с
`available: false`, и селекторы помечают его «(недоступен)» — прятать его нельзя: отсутствие в
списке уже означает «выключен или убран из конфигурации».

| Параметр | Значение | Описание |
|---|---|---|
| `kb.projects[].id` | — | Ключ проекта; обязателен, только строчные буквы, цифры, `. _ -` |
| `kb.projects[].label` | `= id` | Отображаемое имя |
| `kb.projects[].path` | `${PROJECT_PATH}` | Путь к Git-репозиторию |
| `kb.projects[].edit-enabled` | `false` | Разрешить правку рабочего дерева **этого** репозитория. Задаётся для каждого проекта отдельно — общего значения на весь деплой больше нет (для первого проекта в поставляемом yaml подставляется `${KB_GIT_EDIT_ENABLED:false}`). Даже при `true` инструменты записи не действуют, если рабочее дерево недоступно на запись (например, ro-mount); вызов к проекту, который запись не принимает, отклоняется по имени проекта (`GitRegistry.requireEditable`) |
| `kb.projects[].untracked-edit-enabled` | `false` | Разрешить правку **untracked**-файлов — тех, что видны через `allow-globs` ниже. По умолчанию выключено: зона отдаётся только на чтение, и `editFile`, `createFile` и пишущие методы `runScript` на такой файл отвечают отказом. Флаг только сужает `edit-enabled` и никогда его не расширяет: на проекте без `edit-enabled` он ничего не включает, и `ProjectCatalog` пишет об этом в лог при старте (как и о случае, когда `allow-globs` пуст — править нечего). Что разрешено, а что нет, сообщается модели в блоке `<active-project>` (`ProjectPromptService`), чтобы она не тратила вызов на заведомый отказ |
| `kb.projects[].allow-globs` | `[]` | Ant-глобы untracked-файлов **этого** репозитория, которые ассистенту тоже видно: они есть в дереве и в поиске по имени и читаются; правятся только при `untracked-edit-enabled` и в любом случае остаются untracked (правка их не индексирует). **Создавать в этой зоне нельзя**: файлы там производит кто-то другой (сборка, человек), а новый файл либо ушёл бы в индекс и покинул зону, либо остался бы вне git навсегда. Внутри глобов истина — рабочее дерево, git не спрашивают: `.gitignore` эти пути не скрывает (ради этого всё и нужно — отчёты сборки и логи лежат именно в игнорируемом каталоге). Поэтому каждый глоб обязан начинаться с настоящего каталога (`build/reports/**` можно, `**/*.log` — падение на старте): обход укоренён в этом префиксе, и он же ограничивает радиус поражения от опечатки. `grepContent` в эту зону по умолчанию не заходит — нужен `includeUntracked: true`, а `getUncommittedChanges` показывает такие файлы под собственным статусом `U` (не `A`: в индексе их нет и сами в коммит они не попадут). Пустой список — только отслеживаемые файлы, как везде |
| `kb.projects[].git-commands.enabled` | `false` | Разрешить **пользователю** выполнять git-команды над этим репозиторием из интерфейса: чтения о ветках и состоянии, `switch`, `stash`, `commit`, откат файла, `merge --abort`, `fetch`, `pull`. Это не инструмент модели — ассистент ни одной из этих команд не получает, каждая из них выполняется по явному действию человека в панели. Разрешение отдельное от `edit-enabled`: то говорит, что модель вправе писать в рабочее дерево, это — что человек вправе делать с самим репозиторием. Как и с правками, конфигурации мало: команды не предлагаются, если рабочее дерево недоступно на запись (ro-mount), — пишут они все, `fetch` в том числе. Что доступно прямо сейчас, интерфейс спрашивает у `GET /api/git/capabilities` |
| `kb.projects[].git-commands.push-enabled` | `false` | Добавить к ним `push` — единственную команду, которая публикует содержимое репозитория за пределы деплоя, поэтому разрешение на неё своё: «держать checkout свежим» и «вправе отправлять коммиты в remote» — разные права, и первое нужно чаще. Флаг только сужает `git-commands.enabled` и никогда его не расширяет; на проекте без команд он ничего не включает, и `ProjectCatalog` пишет об этом в лог при старте |
| `kb.projects[].enabled` | `true` | `false` — проект не обслуживается и не проверяется: заготовка на будущее |

### Git (`kb.git.*`)

| Параметр | Значение | Описание |
|---|---|---|
| `kb.git.project-path` | — | Прежняя форма записи единственного проекта. Читается только когда `kb.projects` не задан вовсе: тогда из неё собирается проект с `id=default`, всегда read-only — правка включается только записью в `kb.projects[].edit-enabled`. В поставляемом `application.yaml` она подставляется как значение по умолчанию для `kb.projects[0].path`, поэтому деплой, задающий только `PROJECT_PATH`/`KB_GIT_PROJECT_PATH`, работает без изменений |

### Скрипты (`kb.script.*`)

Конфигурация через `ScriptProperties` (`config/model/ScriptProperties.java`) — record с префиксом `kb.script`. Управляет инструментом `runScript` (`ScriptFunction`/`ScriptRunner`): модель пишет один JS-скрипт, который сам обходит репозиторий через объект `kb`, вместо серии отдельных вызовов `grepContent`/`getFileContent`/... Снимок этой конфигурации (без текстов руководств) отдаётся панели «Настройки → Скрипты» через `GET /api/settings/ai-config` (`SettingsController.ScriptSection`, см. [API Reference](api-reference.md)) вместе с пробным запуском (`POST /api/settings/script/run`, `ScriptTestController`) — тем же движком, но всегда read-only.

```yaml
kb:
  script:
    enabled: ${KB_SCRIPT_ENABLED:false}        # выдать ли runScript модели вообще
    edit-enabled: ${KB_SCRIPT_EDIT_ENABLED:true}  # разрешить запись из скриптов (kb.edit/kb.create, kb.writeBytes/kb.createBytes)
    guide: classpath:prompt/script-run.md
    extended-guide: classpath:prompt/script-run-extended.md
    edit-guide: classpath:prompt/script-run-edit.md
    extended-edit-guide: classpath:prompt/script-run-edit-extended.md
    timeout: ${KB_SCRIPT_TIMEOUT:10s}   # бюджет по умолчанию на один прогон
    max-timeout: 30s                    # потолок для timeoutSeconds из вызова
    cancel-poll: 50ms                   # период опроса сторожевого потока
    limits:
      max-files-read: 0      # 0 → дефолт из кода (2000)
      max-bytes-read: 0      # 0 → дефолт из кода (32MB)
      max-calls: 0           # 0 → дефолт из кода (2000)
      max-log-chars: 0       # 0 → дефолт из кода (20000)
      max-result-chars: 0    # 0 → дефолт из кода (20000)
      max-edited-files: 0    # 0 → дефолт из кода (20)
      max-edited-bytes: 0    # 0 → дефолт из кода (256KB)
```

- **`enabled`** — по умолчанию выключено: выполнение присланного кода — поверхность отказа в обслуживании (heap на community-сборке GraalJS ничем не ограничен, см. javadoc `ScriptRunner`), а не только вопрос доступа к файлам. Явный opt-in, как и `kb.mcp.enabled`.
- **`edit-enabled`** — необходимо, но не достаточно: запись из скриптов действует только когда проект принимает запись (`kb.projects[].edit-enabled`) и его рабочее дерево доступно на запись — те же условия, что открывают `editFile`/`createFile` (см. `ScriptEditPolicy`, `GitRegistry.editsAllowed`). Отдельный флаг — чтобы можно было держать обычные инструменты правки включёнными, а скриптам отдавать только чтение. Сверх этого прогон, которому в аргументе `project` назван не проект чата, идёт read-only при любых настройках: назвать чужой репозиторий даёт чтение, но не запись (см. [Инструменты AI](ai-инструменты.md)).
- **`guide`/`extended-guide`/`edit-guide`/`extended-edit-guide`** — руководство в системном промпте, в четырёх комбинациях (справочник × обучающая часть, чтение × запись), собранных один раз при старте (`ScriptGuideService`). Обучающая половина (`extended-*`) добавляется только для прогона, чья модель помечена `weak` (`ChatModelProperties.ModelOption#weak`, см. «Модели чата» ниже) — сильная модель получает только справочник по `kb`, лимитам и видам ошибок.
- **`limits.*`** — бюджеты одного прогона; ограничивают бэкенд (файлы, байты, вызовы `kb.*`, символы в логе/результате, файлы/байты на запись), а не контекст модели — в него попадают только возвращённое значение и лог, у которых свои пределы. Повторный `kb.*`-вызов с теми же аргументами в рамках одного прогона отвечается из кэша и не тратит бюджет (`ScriptSession#call`).

Видимость файлов для скриптов совпадает с остальными инструментами: правила `GitService` — отслеживаемые файлы плюс untracked-файлы, разрешённые `kb.projects[].allow-globs` проекта. Запись — тоже: `kb.edit`/`kb.writeBytes` в untracked-файл проходят только при `kb.projects[].untracked-edit-enabled`, и отказ наступает в момент вызова, до того как прогон разложит по диску свои предыдущие файлы.

Прежние `kb.script.deny-globs` и `kb.script.allow-globs` удалены. Оставшийся в конфигурации ключ Spring просто не связал бы ни с чем, поэтому `RemovedPropertiesCheck` роняет контекст на старте с указанием замены — иначе деплой, считавший, что у него есть ограничение, тихо работал бы без него.

### Мониторинг и остановка чат-рантайма

| Параметр | Значение | Описание |
|---|---|---|
| `kb.chat.monitor-interval-ms` | `60000` | Интервал логирования размеров реестров (≤ 0 — отключить) |
| `kb.chat.shutdown-grace-ms` | `5000` | Сколько ждать завершения отменённых прогонов при остановке приложения, чтобы успело сохраниться частичное сообщение (`ChatRuntimeShutdown`) |

### Эмбеддинги и чанкинг (`kb.embedding.*`)

| Параметр | Значение | Описание |
|---|---|---|
| `kb.embedding.model` | `${AI_EMBED_MODEL}` | Модель эмбеддингов |
| `kb.embedding.reindex-batch-size` | `50` | Документов на batch при `reindexAll()` |
| `kb.embedding.cache.enabled` | `true` | Кэширование эмбеддингов в Postgres |
| `kb.embedding.cache.ttl-days` | `30` | TTL строк кэша (дни) |
| `kb.embedding.cache.cleanup-cron` | `0 0 2 * * *` | Cron-выражение очистки (ежедневно в 02:00) |
| `kb.embedding.chunker.max-tokens` | `512` | Максимум токенов на чанк |
| `kb.embedding.chunker.overlap-tokens` | `64` | Перекрытие токенов между чанками |

### Таймауты и стриминг (`kb.chat.*`)

| Параметр | Значение по умолчанию | Описание |
|---|---|---|
| `KB_CHAT_STREAM_TIMEOUT` | `10m` | Таймаут OpenAI SDK для long-running tool-call раундов и стриминга (применяется к ClientOptions.timeout и OkHttp client) |
| `kb.chat.timeout.sse` | `${KB_CHAT_SSE_TIMEOUT:30m}` | Таймаут SSE-подписки (браузер, окно replay/reconnect) |
| `kb.chat.default-model.id` | `${AI_MODEL}` | ID дефолтной модели для чата |
| `kb.chat.default-model.label` | `${AI_MODEL_LABEL:Default}` | Название дефолтной модели в UI |
| `kb.chat.monitor-interval-ms` | `60000` | Интервал логирования размеров реестров (≤ 0 — отключить) |
| `kb.chat.shutdown-grace-ms` | `5000` | Таймаут ожидания завершения отменённых прогонов при shutdown, чтобы сохранилось частичное сообщение |

### Суммаризация чатов (`kb.chat.summarize.*`)

| Параметр | Значение по умолчанию | Описание |
|---|---|---|
| `KB_CHAT_SUMMARIZE_TOKEN_THRESHOLD` | `30000` | Оценочный вес **сжимаемого среза** (того, что старше живого хвоста), при котором стартует раунд. Меряет срез, а не всё окно |
| `KB_CHAT_SUMMARIZE_MESSAGE_COUNT_THRESHOLD` | `50` | Число сообщений в срезе, при котором стартует раунд. Второй порог той же пары — хватает любого из двух |
| `KB_CHAT_SUMMARIZE_OVERLAP_MESSAGES` | `30` | Недавние сообщения, которые остаются вне суммаризованного окна как живой контекст |
| `KB_CHAT_SUMMARIZE_OVERLAP_USER_MESSAGES` | `5` | Недавние сообщения **пользователя**, остающиеся вне суммаризованного окна. Условие И к предыдущему: хвост обязан удовлетворять обоим. `0` отключает правило |
| `KB_CHAT_SUMMARIZE_SUMMARY_COLLAPSE_THRESHOLD` | `5` | Количество накопленных summary, при котором триггерится collapse в одну meta-summary |
| `KB_CHAT_SUMMARIZE_APPLY_AFTER` | `10m` | Сколько чат должен молчать, чтобы готовая сводка легла в историю. Пауза меряется от последнего ряда чата и нужна из-за кэша промпта: пока он горячий, подмена начала истории стоит полной оплаты следующего запроса, а на остывшем — ничего. Ставить чуть выше времени жизни кэша у провайдера |
| `KB_CHAT_SUMMARIZE_APPLY_AT_RATIO` | `0.5` | Доля окна модели, после которой сводка применяется, не дожидаясь паузы: дальше разговор дорог сам по себе, и потерянный кэш — меньшее из зол. Окно берётся у модели прогона (`context-tokens`); не названо — порога нет, чат ждёт паузы |
| `KB_CHAT_SUMMARIZE_AUTO_COMPACT_AT_RATIO` | `0.8` | Доля окна модели, на которой чат сжимает себя сам перед очередным ответом (`AutoCompactService`) — последний рубеж, заметно выше предыдущего: пара прогонов с инструментами набивает окно быстрее, чем фоновая суммаризация успевает что-то записать, а прогон за пределом окна не отвечает плохо — он не начинается. Запас под самим окном нужен: раунд сжатия читает тот же контекст, который сжимает. Модель без названного `context-tokens` автоматически не сжимается |
| `KB_CHAT_SUMMARIZE_CHARS_PER_TOKEN` | `4` | Примерное количество символов на токен (грубая эвристика без реального токенизатора) |

### Разовые проходы по истории (`kb.chat.backfill.*`)

| Параметр | Значение по умолчанию | Описание |
|---|---|---|
| `KB_BACKFILL_DUMP_PATH` | — (пусто) | Каталог, куда разовый проход кладёт снимок `chat_message` **перед** тем, как что-либо переписать: один NDJSON-файл на запуск, по объекту на ряд, с прежним значением `meta`. Текста переписки в снимке нет — только колонки, из которых проход считает свой ответ (`id`, `conversation_id`, `position`, `type`, `summary`, `summarized`, `meta`) |

Пока путь пуст, проход **не выполняется**. Это не защитная перестраховка, а следствие того, что он делает: единственный сейчас проход (`ProjectStampBackfill`) не переносит готовые значения, а **вычисляет** след проектов из истории, и ошибка вычисления на форме, которой нет ни в одной фикстуре, без снимка неотменяема — прежнего значения колонки после `UPDATE` не существует.

Отказ безопасен: чат без спанов по-прежнему отвечает на «в каком репозитории», просто по `chat_topic` и без диапазонов сообщений (см. `ActiveProjectNotice`). Чтобы проход выполнился, нужен смонтированный каталог на запись — **не** тот, куда идёт экспорт документов: то дерево перезаписывается целиком. В `docker/example.env` под это заведены `KB_BACKFILL_DUMP_PATH` и `BACKFILL_DUMP_PATH_MOUNT`, а в `docker-compose*.yaml` — закомментированный mount.

Проход идёт на уже работающем сервере (`ApplicationReadyEvent`), чат за чатом, каждый — под замком этого чата. Отметка о выполнении ложится в `backfill_state` только если прошли все чаты: сорвавшийся не останавливает остальные, но и «сделано» после него не пишется — на следующем старте проход повторится.

### MCP (Model Context Protocol) (`kb.mcp.*`)

| Параметр | Значение по умолчанию | Описание |
|---|---|---|
| `kb.mcp.enabled` | `${KB_MCP_ENABLED:false}` | Включить/отключить инструменты внешних MCP-серверов. Default off — MCP-серверы могут выполнять локальные команды, требует явного opt-in |
| `kb.mcp.bearer-tokens.*` | — | Bearer-токены для аутентификации удалённых MCP-серверов, ключ должен совпадать с именем соединения (в `spring.ai.mcp.client.*.connections`); применяется как заголовок `Authorization: Bearer <token>` |
| `kb.mcp.headers.<имя соединения>.*` | — | Произвольные дополнительные HTTP-заголовки для соединения (например, `X-Atlassian-Cloud-Id` у mcp-atlassian) — на случай, когда серверу недостаточно одного bearer-токена. Заголовок с именем `Authorization` заменяет собой заголовок, построенный из `bearer-tokens`, а не дублирует его. Заголовок с пустым значением (ключ без значения в YAML, незаданная переменная окружения) не отправляется — так же, как пустой bearer-токен |

## AI-конфигурация

### Переменные окружения

| Параметр | Значение по умолчанию | Описание |
|---|---|---|
| `AI_API_KEY` | — | API-ключ для AI-провайдера |
| `AI_BASE_URL` | `https://api.openai.com/` | Базовый URL AI API |
| `AI_MODEL` | `deepseek-v4-flash` | ID дефолтной модели для чата |
| `AI_MODEL_LABEL` | `Default` | Человекочитаемое название дефолтной модели |
| `AI_EMBED_BASE_URL` | `https://api.openai.com/` | Базовый URL для эмбеддингов |
| `AI_EMBED_API_KEY` | `${AI_API_KEY}` | API-ключ для эмбеддингов (по умолчанию тот же) |
| `AI_EMBED_MODEL` | `bge-m3` | Модель для эмбеддингов |

### Spring AI OpenAI (`spring.ai.openai.*`)

| Параметр | Значение | Описание |
|---|---|---|
| `spring.ai.openai.base-url` | `${AI_BASE_URL}` | Базовый URL OpenAI-совместимого API |
| `spring.ai.openai.api-key` | `${AI_API_KEY}` | API-ключ |
| `spring.ai.openai.chat.options.model` | `${kb.chat.default-model.id}` | Модель чата — ссылается на `kb.chat.default-model.id` |
| `spring.ai.openai.chat.options.max-tokens` | `30000` | Максимальное число токенов в ответе |
| `spring.ai.openai.chat.options.temperature` | `0.1` | Температура (креативность) |
| `spring.ai.openai.chat.options.top-p` | `0.8` | Top-p (nucleus sampling) |
| `spring.ai.openai.chat.options.stream-usage` | `true` | Включение usage-статистики в SSE-потоке |
| `spring.ai.openai.chat.options.extra-body.thinking.type` | `disabled` | Отключение thinking-режима (экономия токенов) |
| `spring.ai.openai.embedding.base-url` | `${AI_EMBED_BASE_URL}` | URL для эмбеддингов |
| `spring.ai.openai.embedding.api-key` | `${AI_EMBED_API_KEY}` | Ключ для эмбеддингов |
| `spring.ai.openai.embedding.embeddings-path` | `/v1/embeddings` | Путь к endpoint эмбеддингов |
| `spring.ai.openai.embedding.options.model` | `${kb.embedding.model}` | Модель эмбеддингов |

### Модели чата (`kb.chat.*`)

Конфигурация через `ChatModelProperties` (`config/model/ChatModelProperties.java`) — record с префиксом `kb.chat`:

```yaml
kb:
  chat:
    default-model:
      id: ${AI_MODEL}              # ID дефолтной модели
      label: ${AI_MODEL_LABEL:Default}  # Название в UI
      weak: ${AI_MODEL_WEAK:true}  # Нужен ли этой модели обучающий раздел runScript
      context-tokens: ${AI_MODEL_CONTEXT_TOKENS:}  # Окно модели в токенах (опционально)
    models:                         # Дополнительные модели (опционально)
      - id: ${KB_CHAT_MODELS_0_ID}
        label: ${KB_CHAT_MODELS_0_LABEL}
        weak: ${KB_CHAT_MODELS_0_WEAK:true}
      - id: ${KB_CHAT_MODELS_1_ID}
        label: ${KB_CHAT_MODELS_1_LABEL}
        weak: ${KB_CHAT_MODELS_1_WEAK:true}
        base-url: ${KB_CHAT_MODELS_1_BASE_URL:}   # Свой эндпоинт (опционально)
        api-key: ${KB_CHAT_MODELS_1_API_KEY:}     # Обязателен, если задан base-url
        stream-usage: true                        # Просить счётчик токенов в стриме
        context-tokens: 128000                    # Окно модели в токенах (опционально)
```

#### Свой хост и токен у отдельной модели

`base-url` и `api-key` у элемента `kb.chat.models` — необязательные. Как это читается:

| `base-url` | `api-key` | Что получается |
|---|---|---|
| не задан | не задан | Обычный случай: модель отвечает с общего эндпоинта `spring.ai.openai.base-url` тем же ключом `spring.ai.openai.api-key` |
| задан | задан | Модель получает собственное соединение: свой HTTP-клиент, свой адрес и свой токен |
| задан | не задан | **Ошибка при старте**: чужой хост с ключом от хоста по умолчанию — комбинация, которой никто не имеет в виду, поэтому приложение не поднимается |
| не задан | задан | Тот же хост, отдельный токен (другой аккаунт или квота) — тоже отдельное соединение |

Соединения строит `ChatModelRegistry` (`config/ChatModelRegistry.java`) один раз на старте, а не на
запрос: у OkHttp-клиента свой пул соединений. Всё, кроме адреса и токена, наследуется от
автоконфигурации — таймаут, ретраи, прокси, заголовки и весь блок
`spring.ai.openai.chat.options.*`, так что второй эндпоинт остаётся вторым эндпоинтом и не
превращается втихую во второй набор параметров генерации. Исключение — признаки провайдера (GitHub
Models, Microsoft Foundry): к другому хосту они не относятся и при заданном `base-url` сбрасываются,
иначе Spring AI переписал бы адрес обратно на адрес провайдера. Провайдер нового хоста по-прежнему
определяется по самому адресу. По id модели выбирается и клиент
(`ChatClientRegistry`, у всех клиентов одна и та же цепочка advisor-ов и один и тот же набор
инструментов), и соединение саб-агента поиска, если `kb.search.subagent.model-id` указывает на такую
модель.

Оба поля работают только внутри `kb.chat.models`. У `kb.chat.default-model` эндпоинт — это и есть
`spring.ai.openai.*`, поэтому `base-url`/`api-key` там отклоняются при старте: связать их было бы не с
чем, а панель настроек показывала бы пилюлю «отдельное подключение» для соединения, которого нет.
Если своё подключение нужно именно модели по умолчанию, добавьте её в `kb.chat.models` с тем же `id` —
такая запись обслуживает и прогоны, где модель явно не выбрана.

Наружу (`GET /api/chats/models`, `GET /api/settings/ai-config`) ни адрес, ни токен не отдаются —
только флаг `ownEndpoint`, по которому «Настройки → Модели» рисуют пилюлю «отдельное подключение».
`toString` у `ModelOption` печатает вместо токена `***`: `@JsonIgnore` закрывает только API, а
свойства попадают ещё и в логи.

`stream-usage` решает, просить ли у эндпоинта этой модели счётчик токенов в стриме
(`stream_options.include_usage`). По умолчанию `true` — без этого поля OpenAI-совместимый эндпоинт
usage в стриме не присылает вовсе, и подпись «сколько токенов ушло на ответ» просто не появится
(см. [Обзор чат-системы](обзор-чат-системы.md), §6.4). Выключать стоит ровно в одном случае: шлюз
отвергает запрос с этим полем и роняет каждый прогон этой модели. Цена выключения — её ответы
остаются без цифры.

`context-tokens` — окно этой модели в токенах. Приложение по нему ничего не запрещает (лимит держит
провайдер), но именно от него считаются автоматические решения о размере истории: доля
`kb.chat.summarize.apply-at-ratio`, после которой готовая сводка перестаёт ждать паузы, и доля
`kb.chat.summarize.auto-compact-at-ratio`, на которой чат сжимает себя сам. Параметр
живёт у модели, а не в общих настройках, потому что окна моделей различаются на порядок, и доля от
чужого окна означала бы либо сжатие впустую, либо сжатие с опозданием. Не задан — у этой модели нет
и порогов: сводка дожидается паузы, автоматически чат не сжимается, и больше ничего не
происходит.

`weak` решает, получает ли модель расширенную половину руководства по `runScript`
(`script-run-extended.md` и её приписку про правки) — см. `ScriptGuideService`. По умолчанию
`true` (безопасное предположение для любой модели, для которой явно не сказано обратное); не
задан — значит модель считается слабой. Раньше это было одно поле деплоя
(`kb.script.extended-guide-enabled`), теперь — свойство каждой модели: сильная модель в списке
экономит контекст, а слабая рядом всё ещё получает подсказки.

### Режимы ассистента (`kb.chat.modes`)

Список готовых «режимов» ассистента (Аналитик, Разработчик, Тестировщик, …) — параллель `kb.chat.default-model`/`kb.chat.models`. Конфигурация через `ChatModeProperties` (`config/model/ChatModeProperties.java`) — record с префиксом `kb.chat`. Каждый режим подставляет свой фрагмент инструкций в системный промпт через плейсхолдер `{mode_instructions}` (см. `prompt/sys.md` и `ChatModeService`); наружу (`GET /api/chats/modes`) отдаются только `id`/`label`, сам текст промпта остаётся на бэкенде.

```yaml
kb:
  chat:
    modes:
      - id: analytic
        label: Аналитик
        prompt: classpath:prompt/mode-analytic.md
      - id: developer
        label: Разработчик
        prompt: classpath:prompt/mode-developer.md
      - id: tester
        label: Тестировщик
        prompt: classpath:prompt/mode-tester.md
```

По умолчанию режим не выбран («без режима» — плейсхолдер `{mode_instructions}` заполняется пустой строкой; фронт хранит и передаёт выбранный режим так же, как модель, см. `PUT /api/chats/{conversationId}/mode` в [API Reference](api-reference.md)).

### Системный промпт (`kb.system-prompt.*`)

Конфигурация через `SystemPromptProperties` (`config/model/SystemPromptProperties.java`) — record с префиксом `kb.system-prompt`. Системный промпт разделён на две части:

- **`prompt`** (reference) — основные правила, таблица выбора инструментов, цепочки вызовов — отдаётся всем моделям
- **`extended-prompt`** (tutorial) — дерево решений, примеры работы, частые ошибки — добавляется только для «слабых» моделей (флаг `kb.chat.default-model.weak` / `kb.chat.models[].weak`)

Обе части подставляются в системный промпт через плейсхолдер `{system_extended}` в `prompt/sys.md` (см. `SystemPromptService` и `ChatRunService`):

```yaml
kb:
  system-prompt:
    prompt: classpath:prompt/sys.md                   # Справочник для всех моделей
    extended-prompt: classpath:prompt/sys-extended.md # Обучение для слабых моделей
```

Стратегия аналогична `kb.script.guide`/`kb.script.extended-guide` (см. выше): слабая модель получает подробные примеры и подсказки, сильная — только краткий справочник, чтобы сэкономить токены.

### ChatConfig — конфигурация чата

`ChatConfig` (`config/ChatConfig.java`) — центральный класс конфигурации чата. Создаёт бины:

- **chatRunExecutor** — `ExecutorService` на виртуальных потоках + `DelegatingSecurityContextExecutorService` (переносит `SecurityContext` на worker-поток). `destroyMethod = "shutdown"`
- **ChatMemory** — своя реализация `ChatHistoryMemory` поверх `ChatHistoryService`: чтение отдаёт несжатое окно истории, запись только дописывает. Размер контекста задаёт не она, а пороги суммаризации `kb.chat.summarize.*`
- **SearchAgentService** — только если `kb.search.subagent.enabled=true`. Создаёт sub-agent с ограниченным набором read-only инструментов из `allowed-tools`
- **ChatToolset** — набор инструментов основного чата: встроенные (`@Tool` из пакета `functions`, каждый в обёртке `RecordingToolCallback`) и пришедшие с внешних MCP-серверов. Собран в одном месте, потому что читателей у него двое: `ChatClient` получает `all()`, а `ToolCatalogService` строит из тех же callbacks каталог для панели «Настройки → Инструменты» — так панель не может показать инструмент, которого у модели нет
- **ChatClient** — основной клиент Spring AI:
  - **ChatModel** — OpenAI-совместимая модель
  - **System Prompt** — загружается через `@Value("classpath:prompt/sys.md") Resource sysPrompt` и передаётся в `.defaultSystem(sysPrompt)`
  - **MessageChatMemoryAdvisor** — автоматически добавляет историю
  - **ToolCallbacks** — `.defaultTools(...)` с 6 ToolCallbacks: `TopicFunction`, `MessageLookupFunction`, `DocumentFunction`, `GitFunction`, `AttachmentFunction`, `SearchAgentFunction` (опционально)

### SecurityConfig — HTTP Basic Auth

`SecurityConfig` (`config/SecurityConfig.java`) — stateless HTTP Basic-аутентификация:

- **PasswordEncoder** — `BCryptPasswordEncoder`
- **InMemoryUserDetailsManager** — один пользователь из `SecurityProperties` (`kb.security.username`/`password`)
- **SecurityFilterChain** — CSRF отключён, `SessionCreationPolicy.STATELESS`, `/actuator/health` публичный, всё остальное требует аутентификации

### SecurityProperties

`SecurityProperties` (`config/model/SecurityProperties.java`) — record с префиксом `kb.security`:

```java
@ConfigurationProperties(prefix = "kb.security")
public record SecurityProperties(String username, String password) {
    public SecurityProperties {
        username = (username == null || username.isBlank()) ? "admin" : username;
        password = (password == null || password.isBlank()) ? "admin" : password;
    }
}
```

### SubAgentConfig

`SubAgentConfig` (`config/model/SubAgentConfig.java`) — record с префиксом `kb.search.subagent`:

```java
@ConfigurationProperties(prefix = "kb.search.subagent")
public record SubAgentConfig(
        boolean enabled,
        String modelId,
        int maxTokens,
        int maxIterations,
        Set<String> allowedTools) {}
```

### sys.md — системный промпт

`prompt/sys.md` — системный промпт ассистента, загружаемый через `@Value` + `Resource`. Определяет:

- **Роль:** «Ассистент для работы с базой знаний»
- **Критические правила:** запрет на выдумку, скрытые инструменты, синтаксис ссылок `[Название](/?doc=ID)`
- **Алгоритм ответа:** вопрос → нужны ли данные → вызов инструмента → ответ
- **Скрытые инструменты:** `recordChatInsights`, `getUserName`, `getCurrentDateTime`, `getOriginalMessages` — вызываются фоново, не упоминаются пользователю
- **Git-репозиторий: общие ограничения** — read-only, только tracked файлы, diff'ы усекаются до 500 строк, файлы >512 КБ возвращаются фрагментом
- **Таблица выбора инструментов** — сопоставление задач и инструментов (вкл. `searchCodebase`)
- **searchFiles vs grepContent** — сравнительная таблица
- **Когда `searchCodebase`, а когда `grepContent`** — правила выбора
- **Цепочки вызовов** — правила оркестрации (поиск → чтение)
- **grepContent: шпаргалка по запросам** — таблица примеров с pattern, pathGlob, regex
- **Ссылки на сообщения** — `[msg:XYZ]` для цитирования позиций в чате

### search-agent.md — промпт sub-agent

`prompt/search-agent.md` — системный промпт search sub-agent, загружаемый через `@Value` + `Resource`. Определяет:

- **Роль:** «специалист по поиску», только чтение
- **Стратегия:** разбить задачу на термины → grep/search → getFileOutline → getFileContent с диапазоном
- **Ограничения git-инструментов**
- **searchFiles vs grepContent** — сравнительная таблица
- **grepContent: шпаргалка по запросам**
- **Жёсткие правила:** не выдумывать, исправлять ошибки аргументов, экономить токены
- **Формат ответа:** Итог, Места (path:line), Связи

## Профиль H2 (`application-h2.yaml`)

Активируется через `SPRING_PROFILES_ACTIVE=h2`. Использует H2 в PostgreSQL-режиме с файловым хранением.

| Параметр | Значение |
|---|---|
| `spring.datasource.url` | `jdbc:h2:file:./data/kb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE` |
| `spring.datasource.driver-class-name` | `org.h2.Driver` |
| `spring.jpa.database-platform` | `org.hibernate.dialect.H2Dialect` |
| `spring.h2.console.enabled` | `true` |
| `spring.h2.console.path` | `/h2-console` |
| `spring.flyway.locations` | `classpath:db/migration-h2` |
| `semantic.enabled` | `false` |

Конфигурация H2 задаётся в классе `H2JdbcConfig` — создаёт `JdbcTemplate` для H2.

## Docker Compose H2 (`docker/docker-compose-h2.yaml`)

```yaml
services:
  backend:
    image: kb-backend-h2
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: kb-backend-h2
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=h2
      - AI_API_KEY=${AI_API_KEY}
      - AI_BASE_URL=${AI_BASE_URL:-https://api.openai.com/}
      - AI_MODEL=${AI_MODEL:-deepseek-v4-flash}
      - AI_EMBED_BASE_URL=${AI_EMBED_BASE_URL:-https://api.openai.com/}
      - AI_EMBED_MODEL=${AI_EMBED_MODEL:-bge-m3}
      - JAVA_OPTS=${JAVA_OPTS:--Xmx200m}
      - PROJECT_PATH_MOUNT=${PROJECT_PATH_MOUNT:-}
      - DOCUMENTS_EXPORT_PATH=${DOCUMENTS_EXPORT_PATH:-../doc-export}
    volumes:
      - kb-h2-data:/app/data
      - ${PROJECT_PATH_MOUNT:-/tmp/kb-project}:/project:ro
      # - ${PROJECT2_PATH_MOUNT}:/projects/billing:ro   # второй проект, если включён
      - ${DOCUMENTS_EXPORT_PATH:-../doc-export}:/app/doc-export
    env_file:
      - ./.env
    restart: unless-stopped

volumes:
  kb-h2-data:
```

## Docker Compose (полный стек, `docker/docker-compose.yaml`)

```yaml
services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: kb-postgres
    environment:
      POSTGRES_DB: knowledgebase
      POSTGRES_USER: knowledgebase
      POSTGRES_PASSWORD: knowledgebase
    ports:
      - "5432:5432"
    volumes:
      - kb-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U knowledgebase -d knowledgebase"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  backend:
    image: kb-backend
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: kb-backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/knowledgebase
      - SPRING_DATASOURCE_USERNAME=knowledgebase
      - SPRING_DATASOURCE_PASSWORD=knowledgebase
      - AI_API_KEY=${AI_API_KEY}
      - AI_BASE_URL=${AI_BASE_URL:-https://api.openai.com/}
      - AI_MODEL=${AI_MODEL:-deepseek-v4-flash}
      - AI_EMBED_BASE_URL=${AI_EMBED_BASE_URL:-https://api.openai.com/}
      - AI_EMBED_MODEL=${AI_EMBED_MODEL:-bge-m3}
      - JAVA_OPTS=${JAVA_OPTS:--Xmx200m}
      - PROJECT_PATH_MOUNT=${PROJECT_PATH_MOUNT:-}
      - DOCUMENTS_EXPORT_PATH=${DOCUMENTS_EXPORT_PATH:-../doc-export}
    volumes:
      - ${PROJECT_PATH_MOUNT:-/tmp/kb-project}:/project:ro
      # - ${PROJECT2_PATH_MOUNT}:/projects/billing:ro   # второй проект, если включён
      - ${DOCUMENTS_EXPORT_PATH:-../doc-export}:/app/doc-export
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - ./.env
    restart: unless-stopped

volumes:
  kb-pgdata:
```

## Файл `.env` / `example.env`

```bash
# AI
AI_API_KEY=your-api-key
AI_BASE_URL=https://api.openai.com/
AI_MODEL=deepseek-v4-flash
AI_EMBED_BASE_URL=https://api.openai.com/
AI_EMBED_MODEL=bge-m3

# Java
JAVA_OPTS=-Xmx200m

# Mounts
PROJECT_PATH_MOUNT=
DOCUMENTS_EXPORT_PATH=

# Второй проект (по умолчанию выключен)
# PROJECT2_ENABLED=true
# PROJECT2_PATH_MOUNT=
```

## Dockerfile (трёхстадийная сборка)

Расположен в `docker/Dockerfile`.

| Стадия | Базовый образ | Назначение |
|---|---|---|
| Stage 1 (build) | `eclipse-temurin:25-jdk-alpine` | Сборка backend (Gradle) и frontend (npm) |
| Stage 2 (extract) | `eclipse-temurin:25-jre-alpine` | Распаковка слоёв JAR для эффективного кэширования |
| Stage 3 (runtime) | `eclipse-temurin:25-jre-jammy` | Финальный образ (git, curl через apt-get, пользователь `kb`) |

Точка входа: `docker/start.sh`.

## Миграции Flyway

### Основные (`src/main/resources/db/migration/`)

| Файл | Описание |
|---|---|
| `V1__init.sql` | Начальная схема: documents, chats, messages, attachments |
| `V2__add_vector_extension.sql` | Включение pgvector |
| `V3__add_embedding_column.sql` | Колонка `embedding vector(1536)` |
| `V4__add_embedding_index.sql` | Индекс для векторного поиска |
| `V5__add_chat_topic.sql` | Колонка `topic` в chats |
| `V6__add_document_history.sql` | Таблица `document_history` |
| `V7__add_document_version.sql` | `@Version` поле в documents |
| `V8__add_summary_fields.sql` | Поля `summary`, `summary_source_version`, `description_version` |
| `V2026.05.25__document_summary.sql` | Миграция summary-полей |
| `V2026.05.26__fix_description_index.sql` | Замена GiST → GIN для description |
| `V2026.05.29__documents_parent_id_index.sql` | Индекс `documents_parent_id_index` на `parent_id` |
| `V2026.05.30__documents_description.sql` | Дроп GIN-индекса `documents_description__index` (мешал HOT-обновлениям). Агрессивный autovacuum на `documents` и TOAST: `scale_factor=0.0`, `threshold=20`, `cost_delay=0` |
| `V2026.06.01__fix_document_history.sql` | Фикс `version = version - 1` в `document_history` |
| `V2026.06.07_00__add_meta_to_chat_message.sql` | Колонка `meta VARCHAR` в `chat_message` (ChatMessageMeta JSON) |
| `V2026.06.07_01__add_model_to_chat_topic.sql` | Колонка `model` в `chat_topic` |
| `V2026.06.09_00__chat_message_keyset_index.sql` | Составной индекс `ix_chat_message_keyset` для курсорной пагинации: `(conversation_id, summary, created_at DESC, id DESC)` |
| `V2026.06.12_00__create_phrase.sql` | Таблица `phrase` + индекс `ix_phrase_category_position` + сид (5 фраз) |
| `V2026.06.20_00__set_default_chat_topic_user.sql` | Переназначение всех существующих чатов на сконфигурированного пользователя (`${default_user}`) |

### H2 (`src/main/resources/db/migration-h2/`)

| Файл | Описание |
|---|---|
| `V1__init.sql` | Начальная схема для H2 (без pgvector) |
| `V2026.06.07_00__add_meta_to_chat_message.sql` | Колонка `meta` в `chat_message` |
| `V2026.06.07_01__add_model_to_chat_topic.sql` | Колонка `model` в `chat_topic` |
| `V2026.06.09_00__chat_message_keyset_index.sql` | Индекс `ix_chat_message_keyset` для курсорной пагинации |
| `V2026.06.12_00__create_phrase.sql` | Таблица `phrase` + индекс `ix_phrase_category_position` + сид (5 фраз) |
| `V2026.06.20_00__set_default_chat_topic_user.sql` | Переназначение пользователя чатов |

## Зависимости

### Backend (`build.gradle`)

| Группа | Зависимости |
|---|---|
| Spring Boot | `spring-boot-starter-web`, `spring-boot-starter-security`, `spring-boot-starter-data-jdbc`, `spring-boot-starter-actuator` |
| Spring AI | `spring-ai-starter-model-openai`, `spring-ai-starter-model-chat-memory-repository-jdbc` |
| Spring Security | `spring-security-core`, `spring-security-config`, `spring-security-web` (6.5.11) |
| Базы данных | `postgresql`, `h2` |
| Миграции | `flyway-core`, `flyway-database-postgresql` |
| Tree-sitter | `tree-sitter`, `tree-sitter-java`, `tree-sitter-python`, `tree-sitter-javascript`, `tree-sitter-typescript` |
| Утилиты | `jackson`, `lombok`, `commons-io`, `guava`, `jspecify`, `httpclient5` |

### Frontend (`package.json`)

| Зависимость | Назначение |
|---|---|
| `react`, `react-dom` | UI-фреймворк |
| `react-markdown`, `remark-gfm` | Рендеринг Markdown |
| `diff` | Diff-представление в истории изменений |
| `i18next`, `react-i18next` | Интернационализация (RU/EN) |
| `i18next-browser-languagedetector` | Автоопределение языка браузера |

## Файлы конфигурации

| Файл | Назначение |
|---|---|
| `application.yaml` | Основная конфигурация |
| `application-h2.yaml` | Профиль H2 (standalone) |
| `docker/Dockerfile` | Трёхстадийная сборка образа |
| `docker/docker-compose.yaml` | Полный стек (PostgreSQL + backend) |
| `docker/docker-compose-h2.yaml` | H2 standalone |
| `docker/start.sh` | Скрипт запуска в контейнере |
| `docker/example.env` | Шаблон переменных окружения |
| `build.gradle` | Зависимости и плагины |
| `settings.gradle` | Настройки Gradle-проекта |
| `frontend/package.json` | Frontend-зависимости |
| `frontend/src/api/client.js` | Единый HTTP-клиент (`request()`, `requestRaw()`, `json()`) |
| `frontend/src/api/chatApi.js` | API-обёртка для `/api/chats/*` (вкл. startRun, stopRun, getActiveRun) |
| `frontend/src/api/chatEvents.js` | SSE-клиент: `openChatEventStream()` с replay и переподключением |
| `frontend/src/api/gitApi.js` | API-обёртка для `/api/git/*` |
| `frontend/src/api/settingsApi.js` | API-обёртка для `/api/settings/*` |
| `frontend/src/icons/index.jsx` | Центральный реестр иконок (~50 иконок) |
| `frontend/src/constants/pagination.js` | Константы пагинации |
| `frontend/src/constants/storage.js` | Константы localStorage |
| `frontend/src/constants/ui.js` | UI-константы |
| `frontend/src/utils/formatting.js` | Утилиты форматирования (`formatFileSize`) |
| `frontend/src/i18n/index.js` | Инициализация i18next |
| `frontend/src/i18n/locales/ru/chat.json` | Русские переводы чата |
| `frontend/src/i18n/locales/ru/common.json` | Русские переводы общих элементов |
| `frontend/src/i18n/locales/ru/knowledgeBase.json` | Русские переводы базы знаний (181 ключ) |
| `frontend/src/i18n/locales/ru/settings.json` | Русские переводы настроек |
| `frontend/src/i18n/locales/en/chat.json` | Английские переводы чата |
| `frontend/src/i18n/locales/en/common.json` | Английские переводы общих элементов |
| `frontend/src/i18n/locales/en/knowledgeBase.json` | Английские переводы базы знаний (181 ключ) |
| `frontend/src/i18n/locales/en/settings.json` | Английские переводы настроек |
