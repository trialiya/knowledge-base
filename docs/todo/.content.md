# TODO

Проработки и планы, по которым код ещё не написан целиком. Завершённые планы
здесь не лежат: как только сделанное описано в `docs/проект/` и
`docs/features/`, документ отсюда уходит — иначе папка превращается в архив,
где не отличить план от истории.

Выпуск, который из проработки уже вырос, ведётся не здесь, а в
`docs/releases/` — по документу на релиз.
